package com.example.pulse.ui.theme

import androidx.compose.ui.graphics.Color

// Semantic aliases used by screens (keeps UI code decoupled from theme internals)
val PulseBackgroundDark = Color(0xFF121212)
val PulseSurfaceDark = Color(0xFF1E1E1E)
val PulseOnSurfaceVariant = Color(0xFF888888)